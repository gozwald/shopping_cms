import React from "react";
import Typography from "@material-ui/core/Typography";

const Products = props => {
  return <Typography>Place of all the Products</Typography>;
};

export default Products;
